import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles((theme) => ({
  referralsContainer: {
    paddingTop: 20,
    paddingLeft: 40,
    paddingRight: 40,
    paddingBottom: 20, 
  },
  containerSeparation: {
    marginBottom: 20
  },
}))
