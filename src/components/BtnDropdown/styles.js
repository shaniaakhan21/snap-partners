import { makeStyles } from '@material-ui/core'

export const useStyles = makeStyles(theme => ({
  iconButton: {
    borderRadius: 0
  },
  usernameText: {
    color: 'white',
    marginRight: 3
  },
  btnAvatar: {
    border: 'none',
    boxShadow: 'none'
  }
}))
